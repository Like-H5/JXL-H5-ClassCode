<!doctype html>
<html lang="zh-cn">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <title>撩课-留言板</title>
    <style>
        a, a:hover {
            text-decoration: none;
            color: #fff;
        }
    </style>
</head>
<body>

<h2 class="text-center mt-5 mb-2">撩课-留言板</h2>
<div class="text-center">
    <a href="/bwl/add" class="btn btn-sm btn-danger">我要留言</a>
</div>

<div class="container mt-5 mb-5">
    <% if (data.length > 0) {
        for (let i = 0, len = data.length; i < len; i++) {
            let item = data[i];
    %>
        <div class="row">
            <div class="card mb-3" style="width: 100%;">
                <div class="card-header d-flex justify-content-between">
                    <div class="card-title">
                        <%=item.title%>
                    </div>
                    <div class="card-deck">
                        <div class="btn btn-danger pt-1 pb-1 pl-2 pr-2 mr-2"><a href="/bwl/del?id=<%=item.id%>">删除</a></div>
                        <div class="btn btn-dark pt-1 pb-1 pl-2 pr-2 "><a href="/bwl/edit/<%=item.id%>">编辑</a></div>
                    </div>
                </div>
                <div class="card-body">
                    <h6 class="card-subtitle mb-2 text-muted">
                        <%=item.currentTime%>
                    </h6>
                    <p class="card-text">
                        <%=item.content%>
                    </p>
                </div>
            </div>
        </div>
    <% } } else { %>
        暂无留言! <a href="/bwl/add">去添加一条留言!</a>
    <% } %>


</div>

</body>
</html>
